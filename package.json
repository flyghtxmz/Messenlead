{
  "name": "messenlead",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check public/app.js && node --check functions/api/messenger/webhook.js && node --check functions/api/messenger/send.js",
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^4.13.2"
  }
}
